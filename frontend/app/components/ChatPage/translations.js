import React from 'react'
import { FormattedMessage } from 'react-intl'

export const SIDEBAR_MENU_ITEM_CONTACTS = (
    <FormattedMessage
        id="sidebar.menu.item.contacts"
        defaultMessage="Contacts"
    />

)
export const SIDEBAR_MENU_ITEM_SETTINGS = (
    <FormattedMessage
        id="sidebar.menu.item.settings"
        defaultMessage="Settings"
    />
)
