import React from 'react'
import { FormattedMessage } from 'react-intl'

export const SERVER_STATE_CONNECTING = (
    <FormattedMessage
        id="server.state.connecting"
        defaultMessage="Connecting"
    />
)

export const SERVER_STATE_CONNECTED = (
    <FormattedMessage
        id="server.state.connected"
        defaultMessage="Connected"
    />
)

export const SERVER_STATE_DISCONNECTED = (
    <FormattedMessage
        id="server.state.disconnected"
        defaultMessage="Disconnected"
    />
)

export const SERVER_STATE_CONNECTION_ERROR= (
    <FormattedMessage
        id="server.state.connection_error"
        defaultMessage="Connection error"
    />
)
