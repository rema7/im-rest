const initialState = {
    urls: {
        chats: '/api/chats',
    },
}

export const settings = (state = initialState, action = {}) => {
    switch (action.type) {
        default:
            return state
    }
}